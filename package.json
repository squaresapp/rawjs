{
	"name": "hut-lib",
	"author": "Paul Gordon",
	"description": "Peacefully simple view layer. Because React is still too complicated.",
	"version": "1.0.4",
	"license": "MIT",
	"main": "hut.js",
	"types": "hut.d.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 0",
		"release": "ts-node ./Build.cover.ts && np"
	},
	"np": {
		"contents": "./+build"
	}
}
